class Exchange {
    constructor(airport, duration, departure) {
        this.airport = airport
        this.duration = duration // int: minutes
        this.departure = departure
    }
}

export default Exchange;